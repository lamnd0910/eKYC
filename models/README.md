# Model artifacts

Model checkpoints are intentionally excluded from version control. Place local
artifacts here only after recording their source, license, checksum and model
version in the experiment log.
