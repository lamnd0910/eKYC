# Dữ liệu

Không lưu ảnh CCCD, ảnh khuôn mặt thật hoặc checkpoint trong Git. Các thư mục
`raw/`, `interim/` và `processed/` chỉ dành cho dữ liệu cục bộ đã được quản lý
quyền truy cập. Khi dùng dữ liệu công khai, ghi rõ nguồn, giấy phép và mục đích
sử dụng trong tài liệu thí nghiệm.
